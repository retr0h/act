---
lint:
  cmd: |
    echo "lint"
format:
  cmd: |
    echo "format"
style:
  cmd: |
    echo "style"
unit:
  cmd: |
    echo "unit"
test:
  cmd: |
    echo "test"
  needs:
    - lint
    - format
    - unit
